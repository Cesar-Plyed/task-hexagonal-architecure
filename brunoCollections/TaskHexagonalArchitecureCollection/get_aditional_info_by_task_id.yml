info:
  name: get_aditional_info_by_task_id
  type: http

http:
  method: GET
  url: http://localhost:8080/api/task/3/aditionalInfo
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
