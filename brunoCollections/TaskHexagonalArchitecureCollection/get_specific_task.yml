info:
  name: get_specific_task
  type: http

http:
  method: GET
  url: http://localhost:8080/api/task/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
