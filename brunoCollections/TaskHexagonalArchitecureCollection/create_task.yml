info:
  name: create_task
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/task
  body:
    type: json
    data: |-
      {
        "title":"Task Tittle",
        "description":"Task Description",
        "completed":"false"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
