info:
  name: delete_task_by_id
  type: http

http:
  method: DELETE
  url: http://localhost:8080/api/task/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
