info:
  name: edit_specific_task_by_id
  type: http

http:
  method: PUT
  url: http://localhost:8080/api/task/3
  body:
    type: json
    data: |-
      {
        "title":"Task with id shared",
        "description":"Description Task",
        "completed":"true"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
